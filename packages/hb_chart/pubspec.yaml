name: hb_chart
description: A Flutter K Chart.
version: 0.0.1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ">=0.18.0 <0.21.0"

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  generate: true